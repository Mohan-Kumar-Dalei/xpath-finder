{
    "name": "xpath-tool",
    "scripts": {
        "build": "cd Frontend && npm install && npm run build",
        "start": "cd Backend && npm install && node server.js",
        "postinstall": "PUPPETEER_SKIP_DOWNLOAD=false node node_modules/puppeteer/install.js"
    }
}